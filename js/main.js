// 5) Inicialización y coordinación
import { renderTareas } from "./dom.js";

// Al cargar la página, renderiza la lista (aunque esté vacía)
document.addEventListener("DOMContentLoaded", () => {
  renderTareas();
});

